package com.fernferret.wolfpound.commands;

public class LocationUtilities {
	protected static String parseWorld(String s) {
		String[] values = s.split(":");
		if(values.length == 2 && values[0].equalsIgnoreCase("w")) {
			
		}
	}
}
